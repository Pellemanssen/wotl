name: Build patch-P v15 Mass Surface MPQ

on:
  push:
    branches: [work/a-little-extra-transfer]
    paths:
      - 'tools/m2_body_patch_v15_mass_surface.py'
      - '.github/workflows/build-patch-p-v15-mass-surface.yml'
  workflow_dispatch:

jobs:
  build-mpq:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/a-little-extra-transfer
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install StormLib MPQ utility
        run: |
          sudo apt-get update
          sudo apt-get install -y smpq
      - name: Generate v15 per-race mass-surface models
        run: python tools/m2_body_patch_v15_mass_surface.py
      - name: Validate geometry safety and anti-tip behavior
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          r=json.load(open('analysis/body_patch_v15_mass_surface_report.json'))
          for race,v in r['races'].items():
              print(race, 'mode',v['mode'], 'changed',v['body_chest_changed'], 'pos',v['body_positive_dx'], 'neg',v['body_negative_dx'], 'apex',v['apex_mean_dx'], 'shell',v['shell_mean_dx'])
              assert v['body_chest_changed'] > 0
              assert v['replacement_chest_changed'] == 0
              assert v['breast_below_chest_low'] == 0
              assert v['only_vertex_xy_changed'] is True
              assert v['z_unchanged'] is True
          # Orc/Scourge must no longer receive the old cone behavior where apex grows more than shell.
          for race in ('Orc','Scourge'):
              v=r['races'][race]
              assert v['shell_n'] >= 8
              assert v['body_negative_dx'] > 0, (race,'expected actual tip pullback')
              if v['apex_n']:
                  assert v['apex_mean_dx'] <= v['shell_mean_dx']*0.80 + 0.002, (race,v['apex_mean_dx'],v['shell_mean_dx'])
          # Draenei is the already-round native-lobe shape, enlarged without belly deformation.
          d=r['races']['Draenei']
          assert d['body_positive_dx'] >= 70
          PY
      - name: Create WotLK MPQ v1
        shell: bash
        run: |
          set -euo pipefail
          rm -rf mpqroot_v15 mpqextract_v15 patch-P.MPQ
          mkdir -p mpqroot_v15
          cp -a generated_v15_mass_surface/Character mpqroot_v15/
          cd mpqroot_v15
          mapfile -t files < <(find Character -type f | sort)
          smpq -c -M 1 -C ZLIB ../patch-P.MPQ "${files[@]}"
          cd ..
          smpq -i patch-P.MPQ | tee analysis/patch-P-v15-info.txt
          smpq -l patch-P.MPQ | tee analysis/patch-P-v15-list.txt
      - name: Validate archive and exact round-trip bytes
        shell: bash
        run: |
          set -euo pipefail
          for f in OrcFemale.m2 OrcFemale00.skin DraeneiFemale.m2 DraeneiFemale00.skin ScourgeFemale.m2 ScourgeFemale00.skin; do
            grep -qi "$f" analysis/patch-P-v15-list.txt || { echo "Missing from MPQ: $f"; exit 1; }
          done
          mkdir mpqextract_v15
          cd mpqextract_v15
          smpq -x ../patch-P.MPQ
          cd ..
          cmp generated_v15_mass_surface/Character/Orc/Female/OrcFemale.m2 mpqextract_v15/Character/Orc/Female/OrcFemale.m2
          cmp generated_v15_mass_surface/Character/Orc/Female/OrcFemale00.skin mpqextract_v15/Character/Orc/Female/OrcFemale00.skin
          cmp generated_v15_mass_surface/Character/Draenei/Female/DraeneiFemale.m2 mpqextract_v15/Character/Draenei/Female/DraeneiFemale.m2
          cmp generated_v15_mass_surface/Character/Draenei/Female/DraeneiFemale00.skin mpqextract_v15/Character/Draenei/Female/DraeneiFemale00.skin
          cmp generated_v15_mass_surface/Character/Scourge/Female/ScourgeFemale.m2 mpqextract_v15/Character/Scourge/Female/ScourgeFemale.m2
          cmp generated_v15_mass_surface/Character/Scourge/Female/ScourgeFemale00.skin mpqextract_v15/Character/Scourge/Female/ScourgeFemale00.skin
          sha256sum patch-P.MPQ | tee analysis/patch-P-v15.sha256
      - name: Upload ready-to-test patch
        uses: actions/upload-artifact@v4
        with:
          name: patch-P-WotLK-Extra-v15-MassSurface
          path: |
            patch-P.MPQ
            analysis/body_patch_v15_mass_surface_report.json
            analysis/patch-P-v15-info.txt
            analysis/patch-P-v15-list.txt
            analysis/patch-P-v15.sha256
          if-no-files-found: error
